{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed:stagetime-plans": "tsx ./src/seed-stagetime-plans.ts",
    "test": "tsx --test ./src/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@replit/connectors-sdk": "0.4.1",
    "stripe": "22.6.1",
    "stripe-replit-sync": "1.0.0"
  }
}
